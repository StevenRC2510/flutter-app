import 'package:flutter/material.dart';

class WindowDetail extends StatelessWidget {
  const WindowDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const <Widget>[
        Text("first"),
        Text("first"),
        Text("first"),
        Text("first")
      ],
    );
  }
}